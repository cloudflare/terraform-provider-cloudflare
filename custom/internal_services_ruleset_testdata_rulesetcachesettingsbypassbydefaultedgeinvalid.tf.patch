diff --git a/internal/services/ruleset/testdata/rulesetcachesettingsbypassbydefaultedgeinvalid.tf b/internal/services/ruleset/testdata/rulesetcachesettingsbypassbydefaultedgeinvalid.tf
new file mode 100644
index 000000000..30838a4ff
--- /dev/null
+++ b/internal/services/ruleset/testdata/rulesetcachesettingsbypassbydefaultedgeinvalid.tf
@@ -0,0 +1,22 @@
+
+resource "cloudflare_ruleset" "%[1]s" {
+  zone_id     = "%[2]s"
+  name        = "%[1]s"
+  description = "set cache settings for the request"
+  kind        = "zone"
+  phase       = "http_request_cache_settings"
+  rules = [{
+    action      = "set_cache_settings"
+    description = "example"
+    enabled     = true
+    expression  = "(http.host eq \"example.com\" and starts_with(http.request.uri.path, \"/example\"))"
+    action_parameters = {
+      cache = true
+      edge_ttl = {
+        mode    = "bypass_by_default"
+        default = 100
+      }
+    }
+  }]
+}
+ 
\ No newline at end of file
