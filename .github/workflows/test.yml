---
name: Test Workflow

on:
  push:
    branches:
      - fix/6924-worker-builds-git-integration

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Test
        run: echo "Hello World"
