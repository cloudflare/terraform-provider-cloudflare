---
page_title: "cloudflare_connectivity_directory_service Resource - Cloudflare"
subcategory: ""
description: |-
  Manages a Cloudflare Connectivity Directory Service.
---

# cloudflare_connectivity_directory_service (Resource)

Manages a Cloudflare Connectivity Directory Service.

Directory services represent applications accessible through Cloudflare tunnels,
acting as a registry for internal services that need to be discoverable and
accessible via Cloudflare's connectivity layer.

## Example Usage

```terraform
# IPv4 host with tunnel network
resource "cloudflare_connectivity_directory_service" "example" {
  account_id = "f037e56e89293a057740de681ac9abbe"
  name       = "my-internal-app"
  type       = "http"

  host {
    ipv4 = "192.168.1.100"

    network {
      tunnel_id = "238dccd1-149b-463d-8228-560ab83a54fd"
    }
  }

  http_port  = 80
  https_port = 443
}

# Hostname host with resolver network
resource "cloudflare_connectivity_directory_service" "hostname_example" {
  account_id = "f037e56e89293a057740de681ac9abbe"
  name       = "my-hostname-app"
  type       = "http"

  host {
    hostname = "internal.example.com"

    resolver_network {
      tunnel_id    = "238dccd1-149b-463d-8228-560ab83a54fd"
      resolver_ips = ["10.0.0.1", "10.0.0.2"]
    }
  }
}
```
<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `account_id` (String) The account identifier to target for the resource.
- `host` (Attributes) The host configuration for the directory service. (see [below for nested schema](#nestedatt--host))
- `name` (String) The name of the directory service.
- `type` (String) The type of directory service. Currently only `http` is supported.

### Optional

- `http_port` (Number) The HTTP port number for the service.
- `https_port` (Number) The HTTPS port number for the service.

### Read-Only

- `created_at` (String) Timestamp when the service was created.
- `id` (String) The identifier of this resource. Same as service_id.
- `service_id` (String) The service ID assigned by Cloudflare.
- `updated_at` (String) Timestamp when the service was last updated.

<a id="nestedatt--host"></a>
### Nested Schema for `host`

Optional:

- `hostname` (String) The hostname of the service.
- `ipv4` (String) The IPv4 address of the service.
- `ipv6` (String) The IPv6 address of the service.
- `network` (Attributes) The tunnel network configuration. (see [below for nested schema](#nestedatt--host--network))
- `resolver_network` (Attributes) The resolver network configuration for hostname-based services. (see [below for nested schema](#nestedatt--host--resolver_network))

<a id="nestedatt--host--network"></a>
### Nested Schema for `host.network`

Required:

- `tunnel_id` (String) The ID of the tunnel to route through.

<a id="nestedatt--host--resolver_network"></a>
### Nested Schema for `host.resolver_network`

Required:

- `tunnel_id` (String) The ID of the tunnel for resolver traffic.

Optional:

- `resolver_ips` (List of String) List of resolver IP addresses.

## Import

Import is supported using the following syntax:

```shell
$ terraform import cloudflare_connectivity_directory_service.example <account_id>/<service_id>
```
